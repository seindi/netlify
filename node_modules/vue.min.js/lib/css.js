require ("core.min.js")
var lib = require ("core.min.js/src")
var $$$ = require ("vue.min.js/library/package.vue")
const {define, echo} = lib
const {zero, one} = lib

define ($$$.vue).function ("css", function (vue) {
	return function () {
		return vue
		}
	})

define ($$$.vue.css).function ("reset", function (vue) {
	return function () {
		return vue
		}
	})
