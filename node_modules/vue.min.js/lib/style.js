require ("core.min.js")
var lib = require ("core.min.js/src")
var $$$ = require ("vue.min.js/library/package.vue")
const {define, echo} = lib
const {zero, one} = lib

define ($$$.vue).function ("style", class {
	constructor (vue) {
		this.vue = vue
		}
	css (data) {
		var style = $$$.vue.fp.style.css
		for (var i in data) style = style.replace (("@").concat (i), data [i])
		var path = "/cgi-bin/style.css"
		this.vue.size_of.push (path, style.length)
		return style
		}
	})
