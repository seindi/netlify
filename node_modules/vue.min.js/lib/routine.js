require ("core.min.js")
var lib = require ("core.min.js/src")
var $$$ = require ("vue.min.js/library/package.vue")
const {define, echo} = lib
const {zero, one} = lib

define ($$$.vue).function ("routine", function () {
	// if (this.socket) this.app.session.reload ()
	// this.app.language = new $$$.vue.language (this)
	// this.account = new $$$.vue.account (this)
	$$$.vue.date ()
	$$$.vue.date.time.call (this)
	return this
	})
