require ("core.min.js")
var lib = require ("core.min.js/src")
const {define} = lib
let $$$ = {express: require ("express.min.js"), app: require ("express.min.js/src"), vue: require ("vue.min.js/library")}
$$$.on = function (... on) { $$$.app.on (... on) }
$$$.emit = function (... emit) { $$$.app.emit (... emit) }
define (module).export ($$$)
