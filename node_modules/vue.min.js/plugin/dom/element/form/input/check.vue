<script>
import vue, {lib, library} from "vue.min.js"
export default vue.next ({
	element: "form:check",
	attribute: ["for", "model"],
	setup: function (prop) {
		var label = vue.reference (prop.for)
		var model = vue.reference (vue.model.format (prop.model))
		if (vue.ini ("watch:legacy")) vue.watch (prop, function () {
			label.value = prop.for
			model.value = vue.model.format (prop.model)
			})
		return {vue, label, model}
		},
	})
</script>

<template id="form:check">
	<input v-if="label" v-bind:for="label" v-model="vue.model.data [model]" v-on:focus="vue.form.focus" v-on:blur="vue.form.blur" type="checkbox" element="form:check">
	<input v-model="vue.model.data [model]" type="checkbox" element="form:check" v-else>
</template>
